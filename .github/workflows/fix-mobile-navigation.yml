name: Fix mobile navigation

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-navigation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Patch navigation without rebuilding the site
        run: |
          python3 <<'PY'
          from pathlib import Path

          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          original = s

          # Mobile menu: keep a real, comfortable area outside the panel.
          s = s.replace(
              '.mobile-nav{width:min(88vw,360px);padding-left:24px;padding-right:24px;}',
              '.mobile-nav{width:min(82vw,340px);padding-left:24px;padding-right:24px;}'
          )

          # Make the overlay explicitly clickable only when open.
          s = s.replace(
              '.nav-overlay{display:none; position:fixed; inset:0; background:rgba(42,21,24,.4); z-index:90; opacity:0; transition:opacity .3s ease;}',
              '.nav-overlay{display:none; position:fixed; inset:0; background:rgba(42,21,24,.4); z-index:90; opacity:0; pointer-events:none; transition:opacity .3s ease;}'
          )
          s = s.replace(
              '.nav-overlay.open{display:block; opacity:1;}',
              '.nav-overlay.open{display:block; opacity:1; pointer-events:auto;}'
          )

          # Replace only the existing mobile-menu block.
          old = '''  // menu mobile
  const burger = document.getElementById('burger');
  const mobileNav = document.getElementById('mobile-nav');
  const overlay = document.getElementById('nav-overlay');
  function closeMenu(){
    burger.setAttribute('aria-expanded','false');
    burger.setAttribute('aria-label','Abrir menu');
    mobileNav.classList.remove('open');
    overlay.classList.remove('open');
    document.body.style.overflow='';
  }
  function openMenu(){
    burger.setAttribute('aria-expanded','true');
    burger.setAttribute('aria-label','Fechar menu');
    mobileNav.classList.add('open');
    overlay.classList.add('open');
    document.body.style.overflow='hidden';
  }
  burger.addEventListener('click', () => burger.getAttribute('aria-expanded')==='true' ? closeMenu() : openMenu());
  overlay.addEventListener('click', closeMenu);
  mobileNav.querySelectorAll('a').forEach(a => a.addEventListener('click', closeMenu));
  document.addEventListener('keydown', (event) => { if(event.key === 'Escape') closeMenu(); });
'''

          new = '''  // menu mobile — correção pontual de UX
  const burger = document.getElementById('burger');
  const mobileNav = document.getElementById('mobile-nav');
  const overlay = document.getElementById('nav-overlay');
  let lockedScrollY = 0;

  function closeMenu(){
    burger.setAttribute('aria-expanded','false');
    burger.setAttribute('aria-label','Abrir menu');
    mobileNav.classList.remove('open');
    overlay.classList.remove('open');
    document.body.style.overflow='';
    document.body.style.position='';
    document.body.style.top='';
    document.body.style.width='';
    window.scrollTo(0, lockedScrollY);
  }

  function openMenu(){
    lockedScrollY = window.scrollY;
    burger.setAttribute('aria-expanded','true');
    burger.setAttribute('aria-label','Fechar menu');
    mobileNav.classList.add('open');
    overlay.classList.add('open');
    document.body.style.overflow='hidden';
  }

  burger.addEventListener('click', () => burger.getAttribute('aria-expanded')==='true' ? closeMenu() : openMenu());
  overlay.addEventListener('click', closeMenu);
  mobileNav.addEventListener('click', (event) => {
    if (event.target.closest('a')) closeMenu();
  });
  document.addEventListener('keydown', (event) => { if(event.key === 'Escape') closeMenu(); });
  window.addEventListener('resize', () => { if(window.innerWidth > 900 && burger.getAttribute('aria-expanded')==='true') closeMenu(); }, {passive:true});

  // Ao recarregar, iniciar sempre no Hero/topo.
  if ('scrollRestoration' in history) history.scrollRestoration = 'manual';
  window.addEventListener('load', () => requestAnimationFrame(() => window.scrollTo(0,0)));
  window.addEventListener('pageshow', (event) => { if(event.persisted) window.scrollTo(0,0); });
'''

          if old not in s:
              raise SystemExit('Existing mobile navigation block not found; no changes made.')

          s = s.replace(old, new, 1)

          if s == original:
              raise SystemExit('No changes were necessary.')

          p.write_text(s, encoding='utf-8')
          PY

      - name: Commit patched index
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "fix: mobile menu and reset page to hero on reload"
            git push
          fi
